# Reliability

Asset.ZIP은 가족 내부용 도구이므로 신뢰성 기준도 실서비스 대비 가볍게 잡는다 (core-beliefs.md 5번 원칙 참고).

## 필요한 것

- **기본적인 데이터 백업**: Vercel Postgres/Neon이 기본 제공하는 백업/복구 기능에 의존한다. 별도의 커스텀 백업 파이프라인은 구축하지 않는다.
- **저장 실패의 명시적 처리**: 사용자가 금액을 입력했는데 저장에 실패했다면, 화면에 조용히 넘어가지 않고 반드시 에러를 표시한다. (QUALITY_SCORE.md와 연결)
- **기본적인 에러 경계**: 특정 화면에서 에러가 나더라도 앱 전체가 하얗게 죽는 것(white screen of death)은 피한다. Next.js의 에러 바운더리 기본 기능 정도면 충분하다.

## 필요 없는 것

- 24/7 가동률 모니터링, 알림 채널(PagerDuty 등) 구성
- 다중 리전 배포, 자동 장애조치(failover)
- 자동화된 부하 테스트, 카오스 엔지니어링
- 별도의 에러 트래킹 SaaS(Sentry 등) 도입 — 필요해지면 그때 가볍게 추가해도 되지만 MVP 단계에서는 우선순위가 아니다.

## 다운타임에 대한 태도

가끔 배포 중 몇 분간 접속이 안 되거나, Vercel/Neon의 일시적 장애로 서비스가 안 되는 것은 허용 가능한 리스크다. 가족 내부 도구이므로 즉각적인 대응 체계를 갖추기보다, 문제가 생기면 다음에 열어볼 때 고쳐져 있으면 충분하다.
