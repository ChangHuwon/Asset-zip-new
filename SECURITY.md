# Security

## 기본 태도: 낮은 위협 수준, 실용적인 방어

Asset.ZIP은 은행 인증정보, 실명, 계좌번호 같은 민감정보를 저장하지 않는다. 저장되는 것은 가족이 직접 입력한 "카테고리 + 금액"뿐이다. 이 데이터가 유출되어도 실질적 피해는 "가족의 대략적 자산 규모가 외부에 알려지는 것" 정도로 제한적이다. 따라서 금융권 수준의 보안 체계(암호화 키 관리, 침투 테스트, 보안 감사)는 이 프로젝트의 우선순위가 아니다. (core-beliefs.md 2번 원칙)

## 그래도 지켜야 하는 최소 기준

- **PIN은 해시로 저장한다.** 평문 저장은 하지 않는다 (bcrypt 등 표준 라이브러리 사용). PIN은 숫자 4자리로 통일한다 (product-specs/new-user-onboarding.md 참고) — 경우의 수가 10,000가지뿐이므로 아래 rate limit이 반드시 함께 적용되어야 안전하다.
- **초대코드는 추측 불가능해야 한다.** 순차 증가하는 숫자나 짧은 문자열이 아니라, 8자 영숫자(혼동되는 0/O, 1/l/I 제외) 랜덤 문자열로 생성한다 (product-specs/new-user-onboarding.md 참고).
- **로그인 시도에는 기본적인 rate limit을 둔다.** PIN이 4자리 숫자(10,000가지 조합)뿐이므로, 무차별 대입을 막기 위한 최소한의 횟수 제한/지연은 반드시 필요하다 (예: 5회 실패 시 일정 시간 잠금).
- **자동 로그인 세션도 최소한의 보호는 갖춘다.** 재방문 시 초대코드/PIN 재입력 없이 자동 로그인되도록 세션을 기기에 저장하지만(product-specs/new-user-onboarding.md 플로우 3), 세션 토큰은 추측 불가능한 랜덤 값이어야 하고 로그아웃 시 즉시 무효화되어야 한다.
- **HTTPS는 기본값을 따른다.** Vercel 배포 시 기본 제공되는 HTTPS를 그대로 사용하고 별도 설정을 하지 않는다.
- **세션/쿠키는 표준 방식을 따른다.** Next.js 생태계의 검증된 인증 라이브러리(예: Auth.js) 관례를 따르고, 직접 암호화 로직을 새로 설계하지 않는다.

## 하지 않을 것

- 실제 은행/증권사 계정과의 연동, 공동인증서·OTP 등 금융 인증 수단 저장
- 접근 로그 상시 감사, 이상 접근 탐지 시스템 구축
- 데이터 암호화 저장(at-rest encryption)의 별도 구현 — 관리형 DB(Vercel Postgres/Neon)가 기본 제공하는 수준에 의존한다.
