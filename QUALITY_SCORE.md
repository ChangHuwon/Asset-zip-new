# Quality Score

Asset.ZIP은 가족 내부용 캐주얼 툴이다 (core-beliefs.md 5번 원칙). 품질 기준도 이에 맞춰 "무엇에 엄격하고 무엇에 관대할지"를 명확히 나눈다.

## 반드시 지켜야 하는 것 (타협 불가)

- **금액 계산의 정확성**: 총액 합산, 카테고리별 비중 계산, 외화→원화 환산 로직은 반드시 정확해야 한다. 이 부분은 간단한 단위 테스트로라도 검증한다. 돈 계산이 틀리는 것은 이 프로덕트에서 유일하게 "치명적"인 결함이다.
- **데이터 유실 방지**: 가족이 입력한 값이 저장 과정에서 조용히 사라지거나 덮어써지면 안 된다. 저장 실패 시에는 사용자에게 명확히 알린다.

## 관대해도 되는 것

- **테스트 커버리지**: 계산 로직 외의 UI 컴포넌트, 화면 흐름은 전수 테스트를 요구하지 않는다. 수동으로 한 번 확인하는 정도로 충분하다.
- **엣지 케이스 처리**: 극단적인 입력값(예: 음수 금액, 비정상적으로 큰 숫자)에 대한 방어는 기본적인 입력 검증 수준이면 충분하고, 모든 경우의 수를 방어할 필요는 없다.
- **성능 최적화**: 가족 몇 명이 쓰는 서비스이므로 대규모 트래픽 대비 최적화는 하지 않는다.

## 판단 기준 한 줄 요약

"이 버그가 가족에게 잘못된 금액을 보여주는가?" — 그렇다면 반드시 고친다. 그렇지 않다면 다음 반복에서 고쳐도 된다.

## 전체 기능 검증 루프 (Full Feature Verification Loop)

기능이 단순해 보여도 "괜찮겠지"라고 넘어가지 않는다. 모든 exec-plan은 `exec-plans/active/`에서 `exec-plans/completed/`로 옮기기 전에 이 루프를 통과해야 한다 (PLANS.md 로드맵 갱신 규칙에도 명시).

### 1. 체크리스트 만들기

해당 단계에 포함된 product-specs의 모든 플로우와 예외 처리 항목을 실제 실행 가능한 테스트 케이스로 나열한다. exec-plan 01(MVP) 기준 체크리스트 예시:

- [ ] 가족 그룹 생성 → 8자 영숫자 초대코드가 발급되는가
- [ ] 초대코드로 합류 → 이름/4자리 PIN 설정이 정상 동작하는가
- [ ] 같은 가족 안에서 이미 쓰이는 표시 이름으로 합류하려 하면 적절히 안내되고 거부되는가
- [ ] 잘못된 초대코드 입력 시 그룹 존재 여부를 노출하지 않고 적절히 안내하는가
- [ ] 로그아웃 없이 재방문 시 자동 로그인되는가 (온보딩 플로우 3)
- [ ] 로그아웃 후에는 다시 초대코드+PIN이 필요한가
- [ ] PIN 로그인 시도 5회 실패 시 rate limit이 걸리는가
- [ ] 카테고리별로 계좌를 최소 1개씩 등록할 수 있는가
- [ ] 원화 계좌 잔액 입력 → 총액에 정확히 반영되는가
- [ ] 외화 계좌 잔액 입력 → 환산된 원화 금액이 미리보기·저장값 모두 정확한가 (원래 금액/통화/환율도 함께 기록되는가)
- [ ] 같은 계좌에 정정 입력 시 이전 값이 아니라 최신 값으로 총액이 갱신되는가
- [ ] 미입력 계좌가 "미입력"으로 구분되고 0원으로 조용히 합산되지 않는가
- [ ] 대시보드 총액 = 모든 계좌 최신 잔액의 합과 수동 계산 결과가 일치하는가
- [ ] 카테고리별 비중 금액/비율의 합이 총액과 정확히 일치하는가
- [ ] 저장 실패(네트워크 끊김 등 시뮬레이션) 시 에러가 명확히 표시되고 값이 조용히 사라지지 않는가

Phase 2 이후 새 exec-plan을 만들 때는 그 단계의 product-specs를 기준으로 같은 방식으로 체크리스트를 새로 뽑는다.

### 2. 반복 절차

1. 체크리스트의 각 항목을 실제로 실행한다 (자동화 테스트가 있으면 그것으로, 없으면 최소한 수동으로 클릭해서 확인한다).
2. 발견된 문제를 이 문서의 기준으로 분류한다.
   - **치명적** (금액이 틀리거나 입력한 데이터가 사라짐 — "반드시 지켜야 하는 것" 위반): 즉시 수정하고, 해당 항목뿐 아니라 체크리스트 전체를 처음부터 다시 돈다.
   - **경미** (UI 어색함, 사소한 엣지 케이스 등): 고치지 않고 [exec-plans/tech-debt-tracker.md](./exec-plans/tech-debt-tracker.md)에 기록한 뒤 계속 진행한다.
3. 체크리스트 전체를 빠짐없이 한 바퀴 돈다.
4. 이번 바퀴에서 치명적 문제가 하나도 없었다면 통과. 하나라도 있었다면 수정 후 1번부터 전체를 다시 돈다.
5. **연속 2바퀴** 동안 치명적 문제가 나오지 않아야 검증 완료로 간주한다. 1바퀴만 통과하고 끝내면 방금 고친 부분이 다른 플로우를 깨뜨렸는지 확인할 기회가 없다.

### 3. 완료 처리

검증 완료 후에만 해당 exec-plan을 `completed/`로 옮긴다. 루프 중 기록만 해둔 경미한 문제는 tech-debt-tracker.md에 남은 채로 다음 단계로 넘어가도 된다 — "관대해도 되는 것" 원칙에 따라 완벽을 요구하지 않는다.
