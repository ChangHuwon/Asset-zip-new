# Frontend

## 기술 스택

- **프레임워크**: Next.js (App Router). Vercel 배포와 궁합이 가장 좋고, PWA 구성 자료도 풍부하다.
- **배포**: Vercel. PWA로 등록해 가족 구성원이 홈 화면에 추가해 앱처럼 사용한다. 별도 iOS/Android 네이티브 앱 스토어 배포는 하지 않는다.
- **차트(2단계 이후)**: Recharts 등 가벼운 라이브러리 하나만 채택한다. 여러 차트 라이브러리를 혼용하지 않는다.
- **스타일링**: Tailwind CSS 권장 — 유틸리티 클래스로 빠르게 반복하기 좋고, 아래 디자인 토큰을 테마로 그대로 옮기기 쉽다.

## PWA 설정 체크리스트

- `manifest.json` (앱 이름: Asset.ZIP, 아이콘, `display: standalone`)
- 기본적인 오프라인 폴백 페이지 (완전한 오프라인 지원은 필요 없음 — 네트워크 끊김 시 안내 화면 정도면 충분)
- 모바일 우선 반응형 레이아웃 (가족 구성원 대부분이 폰으로 접속한다고 가정)

## 디자인 정책: Airbnb 디자인 시스템 기반

Asset.ZIP의 디자인은 [references/design-system-reference-llms.txt](./references/design-system-reference-llms.txt)에 정리된 Airbnb 디자인 시스템을 기반으로 한다. "흰 캔버스 + 단일 포인트 컬러 하나 + 부드러운 라운드 + 최소한의 그림자"라는 Airbnb의 철학이 core-beliefs.md의 미니멀·차분한 톤 방향과 정확히 일치하기 때문에 이를 그대로 정책으로 채택한다. 다만 아래 4가지는 Asset.ZIP에 맞게 조정했다.

1. **폰트를 한글 지원 폰트로 대체한다.** Airbnb Cereal VF/Circular는 한글 글리프가 없고, 원본 문서가 제안하는 대체 폰트(Inter) 역시 한글을 지원하지 않는다. 대신 **Pretendard**를 기본 폰트로 쓰고, 라틴 문자/숫자 표시에는 시스템 폰트가 자연스럽게 겹쳐 보이도록 둔다. 크기/굵기 스케일(디스플레이 20~28px, 굵기 500~600 / 본문 16px, 굵기 400)은 원본 토큰을 그대로 따른다 — 무거운 700+ 굵기를 쓰지 않는다는 원칙이 핵심이지, 폰트 자체가 핵심은 아니다.
2. **포인트 컬러의 의미를 한국 금융 관례에 맞게 재배정한다.** 기존 초안은 "포인트 컬러 = 증가, 레드 계열 = 감소/경고"였는데, 이는 Rausch(#ff385c)를 그대로 포인트 컬러로 쓰면 자기모순이 된다 (포인트 컬러 자체가 레드 계열이므로). 한국 증권/가계부 앱 관례상 **빨강 = 상승, 파랑 = 하락**이므로, Rausch 레드를 CTA와 "증가" 신호의 포인트 컬러로 그대로 쓰고, 감소/경고에는 원본 토큰에 이미 있는 `{colors.legal-link}` (#428bff, 원래는 약관 링크용)를 "하락" 의미로 재배정해 쓴다. 새 색을 추가하지 않고 기존 팔레트 안에서 해결한다.
   - **주의 — 이 재배정은 아직 Phase 1에 적용할 화면이 없다.** "증가/감소"는 이전 값과 비교할 때만 의미가 있는데, Phase 1(대시보드+수동입력)에는 비교 대상이 되는 추이 데이터를 보여주는 화면이 없다 (product-specs/asset-tracking.md 기준 "변동 추이 그래프"는 범위 밖, Phase 2에서 추가). 따라서 **Phase 1의 대시보드 총액 숫자 자체는 `ink`(#222222) 기본 텍스트 색으로 표시하고, Rausch/legal-link 증감 색은 쓰지 않는다** — 이는 Airbnb 원본에서도 `rating-display-card`의 텍스트 색이 Rausch가 아니라 `ink`인 것과 같은 이유다 (평점 숫자도, 우리 총액 숫자도 "포인트 컬러를 아껴 쓴다"는 원칙의 예외가 아니다). 증감 색 토큰은 Phase 2에서 추이 그래프/목표 진행률에 실제 비교 대상이 생겼을 때 처음 쓰인다.
3. **컴포넌트는 마켓플레이스용이 아니라 Asset.ZIP 화면에 맞게 다시 매핑한다.** Airbnb의 검색바, 3-tab 상단 내비게이션, 프로퍼티 카드 같은 컴포넌트는 그대로 쓰지 않는다. 대신 같은 색상/타이포/라운드/그림자 토큰으로 아래 "컴포넌트 매핑" 절의 우리 화면용 컴포넌트를 만든다.
4. **반응형 단계를 4단계에서 2단계로 단순화한다.** Airbnb는 Mobile/Tablet/Desktop/Wide 4단계를 쓰지만, Asset.ZIP은 화면 수 자체가 적고(로그인, 대시보드, 입력 폼) 콘텐츠 밀도도 낮아 Mobile(<744px, 1열)/Desktop(≥744px, 2열) 2단계면 충분하다. Phase 1에서 Desktop 2열은 "총액 카드 + 카테고리 비중 카드"이고, Phase 2에서 추이 그래프가 추가되면 같은 2열 레이아웃의 두 번째 열에 자연스럽게 들어간다 (차트 자체는 위 "차트(2단계 이후)" 항목대로 Phase 1에는 존재하지 않는다).

### 색상

원본 토큰을 그대로 쓰되, 의미만 위와 같이 재배정한다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `primary` (Rausch) | #ff385c | 주요 CTA, 강조 링크, (Phase 2부터) 증가 신호 |
| `primary-active` | #e00b41 | 버튼 press 상태 |
| `primary-disabled` | #ffd1da | 비활성 CTA |
| `legal-link` (재배정: "감소") | #428bff | (Phase 2부터) 감소/하락 신호 — Phase 1에서는 쓰이지 않는다 |
| `ink` | #222222 | 본문/헤드라인 기본 텍스트, **대시보드 총액 숫자(Phase 1)** |
| `body` | #3f3f3f | 긴 설명 텍스트 |
| `muted` | #6a6a6a | 보조 텍스트 (마지막 입력자/날짜 등 메타 정보) |
| `hairline` | #dddddd | 카드/구분선 1px 보더 |
| `canvas` | #ffffff | 기본 배경 |
| `surface-soft` | #f7f7f7 | 비활성 필드, 은은한 배경 |
| `surface-strong` | #f2f2f2 | 원형 아이콘 버튼 배경 |
| `on-primary` | #ffffff | 포인트 컬러 위 텍스트 |

`luxe`, `plus` (Airbnb 서브 브랜드 컬러)는 Asset.ZIP과 무관하므로 채택하지 않는다.

### 타이포그래피

폰트 패밀리만 `'Pretendard Variable', Pretendard, -apple-system, system-ui, Roboto, sans-serif`로 바꾸고, 크기/굵기/행간 스케일은 **대시보드 총액(amount-display) 하나를 제외하고** 원본을 그대로 따른다.

| 용도 | 크기 | 굵기 |
|---|---|---|
| 대시보드 총액 (amount-display) | 40~48px | 700 — 이 시스템에서 유일하게 크고 무겁게 쓰는 지점이라는 *역할*은 Airbnb의 `rating-display`(64px/700, 리스팅 신뢰 신호로 유일하게 타이포를 크게 쓰는 지점)와 같지만, *크기 값 자체는 그대로 가져오지 않고 40~48px로 줄였다*. 원화 총액은 억 단위까지 올라가 자릿수가 많아지므로(예: "128,450,000원"), Airbnb의 4자리 평점("4.81")과 같은 64px을 쓰면 카드 폭을 넘기거나 줄바꿈이 생길 수 있다. |
| 화면 제목 (display-sm~lg) | 20~22px | 500~600 |
| 섹션 제목 (title-md/sm) | 16px | 500~600 |
| 본문 (body-md/sm) | 14~16px | 400 |
| 캡션/메타 정보 (caption, caption-sm) | 13~14px | 400~500 |
| 배지 (badge) | 11px | 600 |

숫자(금액)가 화면의 주인공이므로 금액 표시에는 굵고 큰 폰트를, 설명 텍스트는 작고 옅은 톤으로 위계를 분명히 한다는 기존 방향은 그대로 유지된다 — 이번 변경은 그 방향을 구체적인 px/weight 값으로 확정한 것뿐이다.

### 라운드 / 여백 / 그림자

원본 토큰을 그대로 채택한다.

- **Radius**: 버튼/입력폼 `sm`(8px), 카드 `md`(14px), 초대코드 칩·카테고리 필터·PIN 키패드 같은 pill 요소 `full`(9999px).
- **Spacing**: 4px 기반 스케일(`xxs` 2px ~ `section` 64px) 그대로 사용. 다만 Asset.ZIP은 마케팅 섹션이 없으므로 `section`(64px)은 온보딩/빈 상태 화면 정도에만 쓰고, 카드 내부 패딩은 `lg`(24px), 카드 사이 간격은 `base`(16px)를 기본으로 한다.
- **그림자**: 원본과 동일하게 그림자 1단계 + 무그림자 기본값만 쓴다. `box-shadow: rgba(0,0,0,0.02) 0 0 0 1px, rgba(0,0,0,0.04) 0 2px 6px, rgba(0,0,0,0.1) 0 4px 8px` — 대시보드 카드 hover, 드롭다운 메뉴에만 적용. RELIABILITY.md/QUALITY_SCORE.md의 "과도한 엔지니어링을 하지 않는다"는 방향과도 맞다 — 그림자 단계가 여러 개면 그만큼 유지보수 대상이 늘어난다.
- **다크모드**: Airbnb 공개 웹처럼 다크모드는 만들지 않는다 (명시적으로 스코프 밖).

### 컴포넌트 매핑

Airbnb 토큰을 Asset.ZIP 화면에 매핑하면 다음과 같다.

| Asset.ZIP 컴포넌트 | 근거 토큰 | 설명 |
|---|---|---|
| 총액 카드 | `rating-display` 타이포(단, 40~48px로 축소) + `ink` 텍스트 색 + `rounded.md` + 그림자 1단계 | 대시보드 최상단, 유일하게 큰 타이포를 쓰는 지점. Rausch색은 쓰지 않는다 (Phase 1에는 비교할 증감이 없으므로) |
| 카테고리 비중 카드 | `property-card` 구조 (카드 + `body-sm` 메타) | 카테고리별 금액/비율을 카드로 나열 |
| 계좌 입력 폼 | `text-input` (56px 높이, `rounded.sm`, 포커스 시 ink 2px 보더) | 잔액 입력, PIN 입력 공통 |
| 초대코드 칩 / 카테고리 필터 탭 | `button-pill-rausch`, `rounded.full` | 짧은 텍스트를 강조해서 보여줄 때 |
| 저장/확인 버튼 | `button-primary` | Rausch 배경, 48px 높이 |
| 취소/보조 버튼 | `button-secondary` | 흰 배경 + ink 아웃라인 |
| "마지막 업데이트: 2일 전" 배지 | `guest-favorite-badge` 구조 (작은 pill + `badge` 타이포) | 계좌별 최근 입력 메타 정보 |
| 상단 바 | `top-nav`를 단순화 — 로고(Asset.ZIP) + 계정 메뉴만, 3-tab 내비게이션은 만들지 않는다 | 화면 수가 적어 Airbnb식 다중 탭 내비게이션이 불필요 (컴포넌트 원칙 참고) |

호버 상태 색상, 로딩/스켈레톤 화면, 입력 에러 상태의 보더+헬퍼텍스트 조합은 원본 문서에도 "Known Gaps"로 명시되어 있고 Asset.ZIP에서도 아직 정의하지 않았다 — 구현 착수 시점에 정의한다.

## 컴포넌트 원칙

- 화면 수가 적으므로(로그인/온보딩, 대시보드, 입력 폼, 2단계 이후 추이/목표 화면) 과도한 공통 컴포넌트 추상화를 미리 만들지 않는다. 필요해지면 그때 추출한다.
- 금액 입력 폼은 실수를 줄이는 방향으로 — 통화 단위 표기를 명확히 하고, 외화 입력 시 환산된 원화 금액을 입력 즉시 미리 보여준다.
